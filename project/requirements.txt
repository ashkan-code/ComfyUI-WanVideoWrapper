# Production dependencies — Phase 1 + 2
# All packages are pure Python or have prebuilt ARM64 wheels (Termux-compatible)

fastmcp==3.4.2
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
tenacity==9.1.4
aiolimiter==1.2.1
PyYAML==6.0.1
typing-extensions==4.15.0

# Phase 2 — Data Collection Engine
# On Termux/ARM64: install via `pkg install python-numpy python-pandas` instead
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<2.0.0
